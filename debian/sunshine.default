# Defaults for sunshine initscript
# sourced by /etc/init.d/sunshine
# installed at /etc/default/sunshine by the maintainer scripts

INSTANCEID=0
OPTIONS="--sleep 90 --dagid unstrung --interval 60000 --verbose"
